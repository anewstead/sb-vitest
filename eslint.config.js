/*
To keep workspace tidy the main config is in own folder
ony import/export of that is here
do not add additional config/rules directly here
*/
import rootConfig from "./eslint/eslint.config.root.js";

export default rootConfig;
