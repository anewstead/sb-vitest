import { exampleHelper } from "./componentName.helper";

describe("exampleHelper", () => {
  it("should return exampleHelper called", () => {
    expect(exampleHelper()).toBe("exampleHelper called");
  });
});
